"""
GET /explain/{prediction_id} — endpoint placeholder.
Provides SHAP explanations once the explainer service is wired.
"""

from fastapi import APIRouter

router = APIRouter(tags=["explain"])

# TODO: implement SHAP explanation logic
