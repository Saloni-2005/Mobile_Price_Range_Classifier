"""
GET /feature-importance — endpoint placeholder.
Returns global feature importance metrics from the trained model.
"""

from fastapi import APIRouter

router = APIRouter(tags=["importance"])

# TODO: implement feature importance retrieval
