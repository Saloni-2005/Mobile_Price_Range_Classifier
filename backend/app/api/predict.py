"""
POST /predict — endpoint placeholder.
Provides model prediction once the inference service is wired.
"""

from fastapi import APIRouter

router = APIRouter(tags=["predict"])

# TODO: implement prediction logic
